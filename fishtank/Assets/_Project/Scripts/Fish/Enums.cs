using System.Collections.Generic;
using UnityEngine;

namespace fishtank
{
    public enum SizeClass { Small, Medium, Large }
    public enum LifeStage { Fry, Juvenile, Adult }
    public enum DietClass { Herbivore, Algivore, Carnivore, Omnivore }
    public enum MovementClass { Schooling, Territorial, Benthic, Edge }
    public enum SubstrateType { Sand, Gravel }
}
